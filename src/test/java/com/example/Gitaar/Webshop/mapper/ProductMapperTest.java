package com.example.Gitaar.Webshop.mapper;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@SpringBootTest
@RunWith(SpringRunner.class)
public class ProductMapperTest {
/*
    @Autowired
    private ModelMapper modelMapper;

    @Test
    public void convertToEntity() {
        ProductRequestDto productRequestDto = new ProductRequestDto();
        productRequestDto.setCreator(CREATOR_CHANEL);
        productRequestDto.setProductTitle(PRODUCT_TITLE);
        productRequestDto.setYear(YEAR);
        productRequestDto.setCountry(COUNTRY);
        productRequestDto.setFragranceTopNotes(FRAGRANCE_TOP_NOTES);
        productRequestDto.setFragranceMiddleNotes(FRAGRANCE_MIDDLE_NOTES);
        productRequestDto.setFragranceBaseNotes(FRAGRANCE_BASE_NOTES);
        productRequestDto.setPrice(PRICE);
        productRequestDto.setColor(COLOR);
        productRequestDto.setType(TYPE);

        Product product = modelMapper.map(productRequestDto, Product.class);
        assertEquals(productRequestDto.getCreator(), product.getCreator());
        assertEquals(productRequestDto.getProductTitle(), product.getProductTitle());
        assertEquals(productRequestDto.getYear(), product.getYear());
        assertEquals(productRequestDto.getCountry(), product.getCountry());
        assertEquals(productRequestDto.getFragranceTopNotes(), product.getFragranceTopNotes());
        assertEquals(productRequestDto.getFragranceMiddleNotes(), product.getFragranceMiddleNotes());
        assertEquals(productRequestDto.getFragranceBaseNotes(), product.getFragranceBaseNotes());
        assertEquals(productRequestDto.getPrice(), product.getPrice());
        assertEquals(productRequestDto.getColor(), product.getColor());
        assertEquals(productRequestDto.getType(), product.getType());
    }

    @Test
    public void convertToResponseDto() {
        Product product = new Product();
        product.setId(1L);
        product.setCreator(CREATOR_CHANEL);
        product.setProductTitle(PRODUCT_TITLE);
        product.setYear(YEAR);
        product.setCountry(COUNTRY);
        product.setFragranceTopNotes(FRAGRANCE_TOP_NOTES);
        product.setFragranceMiddleNotes(FRAGRANCE_MIDDLE_NOTES);
        product.setFragranceBaseNotes(FRAGRANCE_BASE_NOTES);
        product.setPrice(PRICE);
        product.setColor(COLOR);
        product.setType(TYPE);

        ProductResponseDto productResponseDto = modelMapper.map(product, ProductResponseDto.class);
        assertEquals(product.getId(), productResponseDto.getId());
        assertEquals(product.getCreator(), productResponseDto.getCreator());
        assertEquals(product.getProductTitle(), productResponseDto.getProductTitle());
        assertEquals(product.getYear(), productResponseDto.getYear());
        assertEquals(product.getCountry(), productResponseDto.getCountry());
        assertEquals(product.getFragranceTopNotes(), productResponseDto.getFragranceTopNotes());
        assertEquals(product.getFragranceMiddleNotes(), productResponseDto.getFragranceMiddleNotes());
        assertEquals(product.getFragranceBaseNotes(), productResponseDto.getFragranceBaseNotes());
        assertEquals(product.getPrice(), productResponseDto.getPrice());
        assertEquals(product.getColor(), productResponseDto.getColor());
        assertEquals(product.getType(), productResponseDto.getType());
    }

 */
}
